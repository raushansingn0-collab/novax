services:
  panel:
    build: .
    restart: unless-stopped
    env_file: [.env]
    ports: ["5000:5000"]
    volumes:
      - ./data:/app/data
      - hosting_sites:/srv/myhosting/sites
volumes:
  hosting_sites:
